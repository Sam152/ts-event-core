{
  "tasks": {
    "init": "git config core.hooksPath .githooks",
    "test": "deno test --allow-run --allow-env --allow-net",
    "test:db-reset": "docker compose -f test/utils/infra/docker-compose.yml down -v",
    "readme": "deno --allow-write ./docs/generateReadme.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/path": "jsr:@std/path@^1.1.1",
    "postgres": "npm:postgres@^3.4.7"
  },
  "fmt": {
    "lineWidth": 110,
    "useBraces": "always"
  }
}
